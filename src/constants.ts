/**
 * The type identifier for primitive (text) elements in the Reakt virtual DOM.
 */
export const PRIMITIVE_ELEMENT_TYPE = "PRIMITIVE_ELEMENT";

/**
 * The type identifier for root element on the DOM.
 */
export const ROOT_TYPE = "CONTAINER_ROOT";
